{
  "name": "imsg-mcp-daemon",
  "version": "1.0.0",
  "description": "iMessage → Cloudflare forwarder daemon",
  "module": "index.ts",
  "scripts": {
    "start": "bun run index.ts",
    "build": "bun run ../build.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "latest"
  }
}
